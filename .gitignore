# Env / secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
next-env.d.ts
out/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build / dist
dist/
build/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Claude Code (lokala inställningar)
.claude/settings.local.json

# Lokala skärmbilder / scratch (jämförelse mot prototypen)
.shots/

# Rå indata (HME-källfiler).
indata/

# Rå ekonomi-CSV (Qlik-uttag per period) — kommunens ekonomidata, versionshanteras
# aldrig. Matas in via scripts/import_ekonomi_serie.py mot import-endpointen.
ekonomi-indata/

# Riktig HME-aggregatdata levereras utanför git (privat; monteras vid deploy).
# Endast katalogen (via .gitkeep) versionshanteras — aldrig själva datafilerna.
/backend/app/data/*
!/backend/app/data/.gitkeep
